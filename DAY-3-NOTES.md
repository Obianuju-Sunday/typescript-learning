# Day 3: TypeScript + Express

## What I learned:
- Setting up Express with TypeScript
- Type-safe routes using Request and Response
- Creating custom User interface
- Using array types (User[])
- Route parameters (:id)
- Array .find() method
- GET all users endpoint
- GET single user by ID endpoint
- POST endpoint to create users
- HTTP status codes (200, 201, 404)

## What I built:
- Express API with TypeScript
- User type/interface
- Mock database (array)
- GET /users - returns all users
- GET /users/:id - returns single user
- POST /users - creates new user

## What I struggled with:
- Understanding the .find() method and arrow functions
- Remembering to add / in routes
- Understanding User[] (array of User type)

## Next:
- PUT/DELETE endpoints
- Request body validation
- Error handling middleware
- Drizzle ORM